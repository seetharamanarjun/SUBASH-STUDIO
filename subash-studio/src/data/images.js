// Placeholder imagery — swap these for real SUBASH STUDIO photography when available.
// Seeded picsum.photos URLs are used so every image loads reliably and stays consistent.
export const img = (seed, w = 1200, h = 1500) =>
  `https://picsum.photos/seed/${seed}/${w}/${h}`;
